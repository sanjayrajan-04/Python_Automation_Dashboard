# Python_Automation_Dashboard
Python Automation Dashboard is a simple Selenium–PyTest automation framework built using Page Object Model (POM). It includes HTML reporting, WebDriverManager support, and Jenkins CI integration to run tests automatically.
